<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.miguelsoler.mipedidos">

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/mipedido"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/mipedido_r"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash_Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.RegisterActivity">
        </activity>

        <activity android:name=".Activity.RecordActivity">
        </activity>

        <activity android:name=".Activity.ProfileActivity" >
        </activity>

        <activity android:name=".Activity.CarritoActivity" >
        </activity>

        <activity android:name=".Activity.PedidosActivity" >
        </activity>

        <activity android:name=".Configs.ScannerActivity">

        </activity>

        <activity android:name=".Activity.LoginActivity">

        </activity>

    </application>

</manifest>